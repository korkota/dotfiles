[core]
  editor = code --wait

[diff]
  tool = vscode

[difftool "vscode"]
  cmd = code --wait --diff $LOCAL $REMOTE

[init]
  defaultBranch = main

[merge]
  tool = vscode

[mergetool "vscode"]
  cmd = code --wait $MERGED

[user]
  email = alexey.korkota@gmail.com
  name = Alexey Korkota
