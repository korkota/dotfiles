[core]
    editor = code --wait

[user]
    email = alexey.korkota@gmail.com
    name = Alexey Korkota

[diff]
    tool = vscode

[difftool "vscode"]
    cmd = code --wait --diff $LOCAL $REMOTE

[merge]
    tool = vscode

[mergetool "vscode"]
    cmd = code --wait $MERGED